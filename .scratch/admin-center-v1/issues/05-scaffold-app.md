# Task: scaffold the app skeleton

Type: task
Status: open

## Question

Stand up the repo skeleton the build slices will land in (stack already locked in the charter): npm-workspaces monorepo with `client/` (Vite + React + TS), `server/` (Node + Hono + TS, better-sqlite3), `shared/` (types), lint + test wiring, a single `npm run dev` that starts both, and a README with run instructions. Use official scaffolding tools (`npm create vite`, etc.) rather than hand-rolling — per claude.md, prefer existing tooling. No feature code: pages and API routes stay empty stubs.

AFK — an agent can do this alone. Resolution records what was created and the dev-run command.
