# UI / information-architecture prototype

Type: prototype
Status: open

## Question

What should the dashboard look and feel like? Build a cheap throwaway prototype (/prototype skill, mock data — no real readers needed) for the user to react to:

- Overall layout: sidebar-with-sections vs single overview page with drill-ins; where the Activity feed lives.
- The per-object **"Ask Claude" icon interaction**: icon placement, pending/running state, how recommendations render (inline expand? side panel?), and how "last checked" + advice history appear.
- One list page (e.g. Projects) and one detail page prototyped concretely; Usage page chart sketches (consult `dataviz` before any chart).
- Visual identity: density, dark/light, iconography per object type.

Resolution = the user has reacted to a concrete prototype and the chosen direction is recorded; the prototype is linked as an asset. Feeds the section build slices (fog) alongside ticket 03.
